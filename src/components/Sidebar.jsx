export default function Sidebar() {
  return (
    <aside className="sidebar">
      <div className="logo">LOGO</div>
      <nav>
        <button className="navbtn">Dashboard</button>
        <button className="navbtn">Imágenes</button>
        <button className="navbtn">Registros</button>
        <button className="navbtn">Usuarios</button>
      </nav>
    </aside>
  );
}
